import { CustomValidarDirective } from './custom-validar.directive';

describe('CustomValidarDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValidarDirective();
    expect(directive).toBeTruthy();
  });
});
